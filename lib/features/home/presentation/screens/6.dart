import 'package:flutter/material.dart';

class P extends StatelessWidget {
  static const String routeName = 'P';


  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurple,
    );
  }
}
