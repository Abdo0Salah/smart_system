import 'package:flutter/material.dart';

class SectionTaps extends StatelessWidget {
  const SectionTaps({super.key});

  @override
  Widget build(BuildContext context) {
    return     Scaffold(backgroundColor:Color(0xffEFF3F7FF),
    );
  }
}
